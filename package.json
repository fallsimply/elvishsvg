{
  "name": "icon-export",
  "version": "1.0.0",
  "description": "Your Figma plugin",
  "main": "code.js",
  "scripts": {
    "build": "esbuild src/code.ts --bundle --loader:.html=file --outdir=plugin --asset-names=[name]",
    "watch": "esbuild src/code.ts --bundle --watch --loader:.html=file --outdir=plugin --asset-names=[name]"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.19.2",
    "esbuild": "^0.9.6",
    "figma-plugin-ds": "^0.1.8",
    "typescript": "^4.2.3"
  }
}
